name: Push

on:
  push:


jobs:
  test:
    # if: |
    #   github.event.pull_request.merged && 
    #   github.ref == 'refs/heads/env/dev' || 
    #   github.ref == 'refs/heads/env/qa' ||
    #   github.ref == 'refs/heads/env/uat' 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: load client
        run: |
          .github/helpers/build.sh b2b ${{ secrets.PAT_TOKEN }}
        shell: bash

      - name: check access to client
        run: |
          cd client
          ls -la
        shell: bash

      - name: pr merged
        run: |
          echo The PR was merged
        shell: bash


          
